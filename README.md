## ⚙️ Setup

To set all configuration files automatically, run the commands below:

```Bash
# These commands should be executed in the Bash interactive shell
$ chmod +x setup        # add permission to execute the script file
$ ./setup               # execute the script file
```
